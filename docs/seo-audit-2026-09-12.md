# SEO + GEO аудит navilet.ru — 12 сентября 2026

Период наблюдения: 30.08–12.09.2026 (13 дней после последних правок).
Источники: логи nginx прода (96 677 запросов, с проверкой ботов по официальным
IP-диапазонам OpenAI и Perplexity), обход всех 57 страниц из sitemap, Lighthouse
(mobile), 15 поисковых запросов с ИИ-сводкой, поиск внешних упоминаний,
HTML сайтов партнёров. Метрика недоступна (нет токена) — сравнение «до/после»
по визитам требует данных владельца.

---

## 1. Трафик после правок — честные цифры

Логи содержат 52 % запросов с браузерным User-Agent, но большая часть — сканеры
уязвимостей. После фильтра «браузер загрузил JS-бандл» остаётся:

| Показатель | Значение |
|---|---|
| Уникальных IP за 13 дней | 269 |
| В будни | ~26 IP/день |
| Просмотров страниц | 998, из них ~350 — 4 IP команды (по 80–180 просмотров в день 03.09 и 08.09) |
| Реальный внешний трафик | ~20–25 посетителей/день, ~45 просмотров/день |
| Смотрят больше одной страницы | 15 % IP |
| Заявок через форму (POST /api/lead 200) | 3 (07.09, 09.09, 10.09) |

Источники по первому внешнему переходу (269 IP):

| Источник | IP | Доля |
|---|---|---|
| Прямой заход / без реферера | 163 | 61 % |
| Яндекс | 66 | 25 % |
| Google | 19 | 7 % |
| lk.navilet.ru | 10 | 4 % |
| ChatGPT (реферер + utm_source=chatgpt.com) | ~9 | 3 % |
| Perplexity, VK, e.mail.ru, сторонние сайты | по 1–2 | — |

Выводы:

- Органика даёт ~6–8 визитов в день. Позиции по нишевым запросам топовые
  (см. раздел 3), но частотность самих запросов низкая — потолок ниши.
- **Рассылки не видны.** UTM-меток у писем нет (единственные метки —
  `aimpact-plus` и `chatgpt.com`), поэтому переходы из писем падают в «прямые».
  Всплески 03.09 и 08.09 — не рассылки, а просмотр сайта командой.
- ChatGPT уже приводит людей: ~9 визитов за 13 дней при нулевых затратах.

## 2. Боты: кто реально читает сайт

85 % всех запросов — сканеры. 93 % запросов с User-Agent ИИ-ботов — подделки
(ходят за `/.env`, `/laravel/.env`, `/.aws/credentials`). Проверено по IP:

| Бот | Настоящих запросов к страницам | Дней активности | Что читает |
|---|---|---|---|
| ChatGPT-User (живые ответы пользователям) | 69 | 13 из 13 | `/` 44, `/integraciya-tourvisor` 5, `/tarify` 3, `/versii`, `/vozvrat-klientov` |
| OAI-SearchBot (индекс поиска ChatGPT) | 85 | 9 | robots 55, `/`, `/spros/vetnam`, `/spros/shri-lanka`, `/versii` |
| GPTBot (обучение) | 47 | 4 | sitemap 13, `/resheniya/*`, `/karta-sayta` |
| PerplexityBot | 65 | 10 | `/skolko-stoit`, `/o-komande`, `/tarify`, `/podborki` |
| Perplexity-User | 0 | — | все 725 запросов — подделки |
| Claude-User | 34 | — | `/` 18, `/blog/gde-brat-lidy-turagentstvu` 9, `/dlya-turagentstv` 4 |
| ClaudeBot | sitemap 157 раз | 14 | индексирует регулярно |
| Googlebot (настоящий, 66.249.x) | 277 | — | обошёл 51 из 57 страниц |
| YandexBot | 212 | — | обошёл 55 из 57 (нет `/blog/skolko-stoit-ii-dlya-turagentstva`, `/spros/tailand`) |
| bingbot | 196 | — | индекс для ChatGPT Search |

- **llms.txt и llms-full.txt за 13 дней не запросил ни один ИИ-бот** — только
  люди и PR-CY. Файлы держим, но опираться на них нельзя: факты модели берут
  из обычных страниц.
- ChatGPT-User на главной 44 раза = живые ответы пользователям с нашей
  страницей в источниках. Это прямое подтверждение цитируемости.

## 3. Видимость в поиске и ИИ-сводках (замер №2)

| № | Запрос | Позиция navilet.ru | ИИ-сводка называет бренд | Кто выше / вместо нас |
|---|---|---|---|---|
| 1 | ИИ-ассистент для турагентства подбор туров | **№1**, №2, №5 | да, с ценой 990 ₽ | tourvisor.ru, PapAI Soft (49 000 ₽) |
| 2 | сколько стоит чат-бот для турагентства | **№1** `/skolko-stoit` | да, наша цена первой | voxys.ru, papaisoft.com, lubava.ai |
| 3 | лиды для турагентства где брать клиентов | **№1, №2** (было: отсутствие) | косвенно, цитирует страницы без бренда | dtf.ru, vc.ru, divitio.ru |
| 4 | виджет подбора туров на сайт | **выпали из топ-5** (было №3) | нет | tourvisor.ru №1–5 |
| 5 | Навылет AI отзывы | вся выдача наша | да; **«независимых отзывов не обнаружено»** | — |
| 6 | цифровизация турагентства | **№2** блог | да, по имени | atorus.ru №1 |
| 7 | автоматизация турагентства 2026 | **№1**, №3 | да, по имени | noltis.ru, sevenrelay, papaisoft |
| 8 | нейросети в туризме примеры внедрения | **№1** кейс МГП | косвенно; по имени названы Just AI, GigaChat, «Трэвик» | gptmag.ru, atorus.ru |
| 9 | ИИ для туроператора | **№1**, №3 | косвенно | neuraiq.dev, zvonobot.ru |
| 10 | чат-бот для турагентства | **нет** | нет; названы Noltis, U-ON | superintellect, nextbot, neurobot, noltis, tourbot |
| 11 | как турагентству увеличить продажи | **нет** | нет | amsales, turmarketing, CRM-блоги |
| 12 | ИИ-ассистент в MAX для бизнеса | **нет** | нет | max-osnova.ru, dev.max.ru |
| 13 | интеграция Tourvisor ИИ | **№2**, №3 | да, по имени | api.tourvisor.ru |
| 14 | как турагентству возвращать клиентов | **нет** | нет | kultura101, mindbox, kokoc |
| 15 | ИИ для сети турагентств | **№1**, №2 корпсайт, №3 кейс | да, по имени | noltis.ru, atorus.ru |

Итог: №1 в 7 запросах из 15, бренд назван в ИИ-сводке в 7 из 15, ещё в 3
цитируемся без имени. Цена «от 990 ₽» транслируется верно везде. Рост по лидам
(с нуля до №1–2). Падение по виджету. Пять широких тем закрыты конкурентами.
Новые конкуренты, которых стоит отслеживать: noltis.ru, papaisoft.com,
superintellect.ru, «Трэвик».

## 4. Внешние ссылки — критический пробел

**Подтверждённых обратных ссылок на navilet.ru — ноль.**

| Площадка | Что есть | Проблема |
|---|---|---|
| incrussia.ru (колонка Пустова, РСТ) | «Навылет» и ELARTEX | без «AI», без ссылки |
| techora.ru | перепечатка Inc. | без ссылки |
| tourismexpo.ru (спикер Силагадзе, ОТДЫХ 2026) | «сооснователь проекта Навылет!» | без ссылки |
| atorus.ru, hotel.report (интервью 2025) | кейс МГП + Турвизор | **бренд не назван, фамилия с опечаткой «Сигаладзе»** |
| profi.travel (Агафонов, МГП) | «чат-бот подбирает тур» | продукт и разработчик не названы |
| gubkin.tpprf.ru (заседание Совета ТПП) | анонс | Навылет/ИИМПАКТ не упомянуты |
| rusprofile.ru | реквизиты | «сайт отсутствует», **нет товарного знака** |
| VC.ru | статья должна быть опубликована | **в индексе не находится** — проверить публикацию |
| navigator.sk.ru | профиль резидента | **не найден** |
| Startpack, CMS Magazine, Рейтинг Рунета, Product Radar | — | карточек нет |
| Сайты партнёров (mgp.ru и др.) | только `<script>` виджета | **виджет в iframe → ссылки нет** |

Все подтверждения статусов (ТПП, РСТ, Сколково) существуют только на нашем
сайте — для поисковиков и моделей это самозаявления.

## 5. Технические находки

### Критично (влияет на индексацию и ранжирование главной)

1. **H1 главной склеен.** Анимация по словам рендерит `<span>` без пробелов;
   textContent = «ИИ-ассистенттурагентства:отвопросатуриста—доготовойзаявки».
   Так его читают краулеры без JS (ChatGPT-User, OAI-SearchBot, Яндекс до
   рендера) и скринридеры. Ключевая фраза страницы в индексе искажена.
2. **LCP 4,4 с на мобильном** (норма < 2,5 с). LCP-элемент — абзац hero с
   `opacity:0` в статическом HTML: текст появляется только после гидратации
   и анимации. Lighthouse: Performance 75, SEO 92, A11y 91, Best Practices 79,
   вес 737 KiB, неиспользуемого JS 107 KiB.
3. **Дубли адресов отдают 200:** `/tarify.html`, `/index.html`,
   `/blog/*.html`, а также весь сайт по `https://5.129.241.199/`
   (445 переходов с реферером IP за период). Canonical стоит, но это
   размывает краулинговый бюджет и даёт мусорные хосты в индексе.
4. **`/keisy/mgp`: dateModified = 2026-11-27** — дата из будущего, автора нет.
   Поисковики перестают доверять датам такого источника.

### Важно

5. **Meta description длиннее 165 символов на 25 страницах** (до 308 на
   `/podborki`, 257 `/dlya-turagentstv`, 252 главная) — сниппеты обрываются
   на середине фразы. Title длиннее 70 на 8 страницах.
6. **lastmod в sitemap устарел:** 13.08 и 19.08 при правках 30.08 на главной,
   `/start`, `/prognozy`, `/voprosy`, `/lidy-dlya-turagentstva`, `/skolko-stoit`.
   Ненадёжный lastmod поисковики игнорируют целиком. Дата в шапке llms.txt
   также 19.08.
7. **robots.txt:** `Disallow: /_next/` для `User-agent: *` закрывает CSS/JS
   от bingbot (индекс ChatGPT Search) и остальных. `Crawl-delay` Google
   игнорирует, Яндекс не учитывает с 2018 года.
8. **Нет RSS** — нет импорта в Дзен, агрегаторы и часть ИИ-краулеров.
9. **E-E-A-T:** автор блога — Organization, не Person; в Organization нет
   `founder`; `sameAs` — только Telegram и ЛК; у SoftwareApplication нет
   `aggregateRating` (и отзывов, на которые он мог бы опереться).
10. **Ноль внешних ссылок-источников в статьях блога** — все цифры без
    ссылок на АТОР, Росстат, Tourvisor, исследования. Для генеративных
    движков источники — главный сигнал цитируемости.
11. **Тонкие хабы:** `/resheniya` 190 слов, `/vidzhet` 243, `/o-komande` 294.
12. **Слабая контекстная перелинковка:** `/karta-sayta` 0 входящих из
    контента, `/dlya-turoperatorov` 1, `/resheniya` 1, `/dlya-setey-agentstv`,
    `/voprosy`, `/o-komande`, `/dashboard` по 2; `/demo` — 0 исходящих.

### Мелочи

13. Пиксель Метрики `<img>` без `alt` на всех страницах.
14. Кнопка отправки в чате без aria-label; контраст; `h4` в футере без `h2/h3`.
15. og:image 1376×768 вместо 1200×630.
16. 9 Event-схем прошедших мероприятий без `endDate`/`eventStatus`.

Что в порядке: все 57 страниц 200, canonical везде верный, `lang=ru`,
верификации Google и Яндекса, редиректы http→https и www→без www,
слэш→без слэша, 404 отдаёт 404, чувствительные пути закрыты, answer-капсулы
на ключевых страницах (26–59 слов), JSON-LD без ошибок парсинга, FAQPage на
34 страницах, Dataset на `/spros/*`, HowTo на `/vidzhet/*`.

## 6. Контентные пробелы (по фактической выдаче)

| Тема | Состояние | Что нужно |
|---|---|---|
| «чат-бот для турагентства» — главный коммерческий запрос | нас нет; топ у superintellect, nextbot, neurobot, noltis, tourbot | посадочная `/chat-bot-dlya-turagentstva`: ИИ-ассистент как чат-бот нового поколения, сравнение с кнопочными |
| «виджет подбора туров» | выпали, Tourvisor №1–5 | усилить хаб `/vidzhet` (243 слова): сравнение «модуль Tourvisor vs ИИ-виджет», когда что выбирать |
| MAX для бизнеса | канал есть, страницы нет | `/max` — «ИИ-ассистент турагентства в MAX: подключение, сценарии» (тема горячая, конкуренция низкая) |
| «как возвращать клиентов» | продуктовая страница есть, информационной нет | статья-инструкция в блог + ссылка на `/vozvrat-klientov` |
| «как турагентству увеличить продажи», «цифровизация турагентства» | одна статья | pillar-гид «Цифровизация турагентства 2026» + чеклист + глоссарий терминов |
| Сравнения | план A4 не выполнен | «ИИ-ассистент vs самописный бот vs аутсорс», «Навылет vs Noltis vs модуль Tourvisor» — таблицы цитируются моделями лучше прозы |
| Кейсы | один (МГП) | Sun Orange, AnyTour, AeroTour — с цифрами; кейсы = ссылки от клиентов |
| Данные | `/spros/*` статичны | ежемесячный «Индекс спроса Навылет» на реальных 175 000+ сообщений — PR-повод и магнит для ссылок из СМИ |
| Отзывы | нет ни одного внешнего | `/otzyvy` с реальными отзывами → aggregateRating; карточки в Отзовик, Яндекс Бизнес |
| Автор-эксперт | нет страницы | страница Лукиана Силагадзе с Person-схемой, `sameAs` на tourismexpo, ТПП, рейтинг «Генерального директора» |
| Частота публикаций | последняя статья 19.08, в сентябре ноль | 2–4 материала в месяц, каждая статья с 3–5 внешними источниками |

## 7. Ссылочная стратегия — приоритеты

1. **Виджет.** Сейчас партнёр вставляет только `<script>`; ссылка «Powered by»
   живёт внутри iframe и принадлежит lk.navilet.ru. Добавить в сниппет
   вставки статичный `<a href="https://navilet.ru/…">Подбор туров с ИИ —
   Навылет! AI</a>` (или инжектить якорь в светлый DOM партнёра) — 30+ доменов
   турбизнеса дадут ссылки за один релиз виджета.
2. **Исправить существующие упоминания:** АТОР и Hotel.Report — фамилия и
   название продукта со ссылкой; Inc.Russia — «Навылет! AI» и ссылка;
   tourismexpo.ru — ссылка в карточке спикера. Это уже индексированные
   страницы доверенных СМИ.
3. **Сколково:** завести/опубликовать профиль в navigator.sk.ru с сайтом.
4. **Каталоги и отзывы:** Startpack, CMS Magazine, Рейтинг Рунета, Product
   Radar, TAdviser, Otzovik; Яндекс Бизнес + 2ГИС (карточка компании с сайтом
   и отзывами закрывает и «отзывы», и локальный сигнал для Яндекса).
5. **ТПП РФ / РСТ:** попросить публикацию о рабочей группе по ответственному
   ИИ с упоминанием проекта и ссылкой; письмо №49К/0009 как повод.
6. **Pressfeed.ru:** отвечать на запросы журналистов по ИИ/туризму — самый
   дешёвый канал ссылок из СМИ в рунете.
7. **VC.ru, Habr, Дзен:** проверить публикацию первой статьи; вторая готова
   (`docs/vc-article-lidy.md`); Дзен — через RSS после его появления.
8. **Партнёры и Tourvisor:** ссылка с сайтов клиентов из кейсов; листинг
   в партнёрах Tourvisor.
9. **Wikidata + товарный знак:** сущность бренда для knowledge graph;
   регистрация ТЗ защищает бренд и подтверждает его в реестрах.

## 8. Нужно от владельца

- Метрика: скриншоты «Источники → Сводка» по неделям за 01.08–12.09 или
  OAuth-токен — без этого нет сравнения «до/после».
- Вебмастер: скриншот «Видимость сайта в Алисе AI» и «Индексирование →
  Страницы в поиске» (сколько страниц в индексе, есть ли `.html`-дубли).
- URL опубликованной статьи на VC.ru.
- UTM-метки в рассылки: `?utm_source=email&utm_medium=newsletter&utm_campaign=<дата>`.
- Контакты редакций АТОР / Inc. / tourismexpo для правок; 2–3 клиента,
  готовых дать отзыв.

## 8а. Итерация 1 выполнена (12.09.2026, коммиты 9d7f1b9, 891c1c8)

Замер Lighthouse mobile с реальным троттлингом 4G, главная страница:

| Метрика | До | После |
|---|---|---|
| LCP | 7,1 с | 2,6 с |
| FCP | 3,5 с | 2,6 с |
| CLS | 0,12 | 0,07 |
| Speed Index | 4,8 с | 3,0 с |
| A11y | — | 95 |

Что сделано: пробелы между словами H1; hero без `opacity:0` (варианты
`heroWordVariant`, `heroRise`, `heroScaleIn`); единый источник высоты и ключа
промо-плашки (`src/lib/promoBanner.ts`) — убраны два сдвига макета при
загрузке; 301 для `*.html`, `/index.html` и чужих хостов, включая IP;
`dateModified` МГП; Event-схемы с `endDate`/`performer`; 33 description ≤160 и
6 title ≤70; `lastmod` и дата llms.txt из git при сборке
(`scripts/gen-lastmod.py`, шаг в deploy.sh); robots без `Crawl-delay` и с
открытым `/_next/static`; RSS `/blog/rss.xml` + `rel=alternate`; Organization
получила `founder` (Person) и Rusprofile в `sameAs`; новая og-image 1200×630;
aria-label кнопки чата; корректные MIME для sitemap, RSS и llms.txt.

Не делалось (нужно решение владельца): автор блога остаётся Organization —
переключение на Person одной строкой после подтверждения, что автор статей
Лукиан Силагадзе.

Побочный эффект генератора дат: правка description затронула 49 страниц,
и их lastmod сдвинулся на 12.09. Дальше даты будут двигаться только при
реальных правках файлов страниц.

## 8б. Метрика подключена (12.09.2026, OAuth-приложение «Navilet audit», токен в `.env.local`)

Счётчик 108200337, роботы отфильтрованы. За 30.08–12.09: 231 визит (16,5/день),
188 посетителей, органика 87 визитов (Яндекс 47, Google 40), 3 заявки — те же
даты, что POST /api/lead в nginx. Динамика: июль 11,7 визита/день → август
13,9 → сентябрь 17,4. Воронка периода: форму открыли 15 человек, начали
заполнять 3, отправили 3 — обрыв на первом поле. UTM: chatgpt.com 8 визитов
за период (15 всего), aimpact-plus 41 визит без заявок, email только
июльская `jul_demo` (2 человека). Цель `hero_chat_version_switch` — 0
срабатываний за всё время, проверить. Скрипты выгрузки — разовые, в репозиторий
не входят.

## 8в. Итерация 2 — контент-ядро (12.09.2026)

Что добавлено:

- **`/chat-bot-dlya-turagentstva`** — посадочная под главный коммерческий
  запрос, где нас не было: требования к боту в 2026, 4 типа ботов с ценами
  (из `costAlternatives`), пример диалога, каналы, версии, FAQ (7) с FAQPage
  и Service-схемой. ~1 400 слов.
- **`/max`** — «ИИ-ассистент турагентства в MAX»: аудитория MAX по Mediascope
  и VK (86,2 / 108 млн), 156-ФЗ и предустановка с 01.09.2025, платформа для
  бизнеса (юрлица/ИП, модерация бота), что делает ассистент в чате, сценарий,
  таблица «сайт или MAX», подключение, цена второго канала из
  `crossChannelAddons`, FAQ (7), 7 внешних источников. ~1 800 слов.
- **`/sravnenie`** — таблица по 13 критериям: «Навылет! AI» / модуль поиска
  Tourvisor / конструктор ботов и ИИ-агент / разработка на заказ. Названы
  конкретные сервисы и цены по открытым прайсам на 12.09.2026 (Tourvisor,
  СуперИнтеллект, NextBot, Нейробот, TourBot, Noltis, PapAI Soft) со ссылками
  `rel="nofollow"` и дисклеймером. FAQ (6). ~1 600 слов.
- **`/vidzhet`** 243 → ~1 090 слов: что делает виджет, таблица «форма поиска
  vs диалог», код и настройки, FAQ (6) + FAQPage.
- **`/resheniya`** 190 → ~800 слов: четыре узла автоматизации с цифрами сети,
  «с чего начать», как измерить, FAQ (5) + FAQPage.
- **Блог:** новая статья «Как турагентству возвращать клиентов: 7 механик»
  (~1 600 слов, 9 внешних источников). В `ii-v-turizme-2026` и
  `gde-brat-lidy-turagentstvu` добавлены абзацы с данными АТОР, Mediascope,
  «Слетать.ру», New Point и разделы «Источники».
- Перелинковка: меню «Продукт → Подключение → Ассистент в MAX», футер
  («Ассистент в MAX», «Чат-бот для турагентства», «Сравнение»), карта сайта,
  llms.txt / llms-full.txt, `/vozvrat-klientov` → `/max`, `/skolko-stoit` →
  `/sravnenie`, `/resheniya/chat-bot-ne-rabotaet` → посадочная, статьи блога.
- Sitemap: +3 маршрута, `gen-lastmod.py` знает новые страницы.

Факты о конкурентах и MAX собраны субагентом по открытым источникам
12.09.2026; что не подтвердилось (интеграция Noltis с Tourvisor, собственный
ИИ-чат Tourvisor, цены MAX для бизнеса) — на страницы не попало.

Не сделано в этой итерации: «Индекс спроса», кейсы Sun Orange/AnyTour,
страница эксперта, `/otzyvy` — это итерация 4 и требует данных от владельца.

## 8г. Итерация 3 — видимость воронки (13.09.2026)

Главная находка: **воронка регистрации не измерялась вообще.** На
`lk.navilet.ru` код счётчика в HTML есть, но заголовок CSP
(`script-src 'self'; connect-src 'self'`) не даёт загрузить `tag.js` и
отправить данные. За всё время Метрика записала на кабинете 4 просмотра,
тогда как одну кнопку регистрации с июля нажали 58 уникальных человек.

Следствие для прошлых выводов: цифра «14 заявок за всё время» описывает только
путь через форму. Второй путь — самостоятельная регистрация — вне измерений, и
разрыв «15 открыли форму → 3 заполнили» частично объясняется не потерей людей,
а их уходом по кнопке «Подключусь сам» на первом шаге формы. Переделывать форму
до починки аналитики бессмысленно.

Что сделано на стороне сайта:

- `src/lib/lk.ts` — единый сборщик ссылок в кабинет с параметром `from`
  (`lkRegisterUrl`, `lkLoginUrl`). Проставлен на всех шести переходах:
  форма, лендинг `/start` (верх и низ), демо (верх и низ), вход в шапке
  (десктоп и мобильное меню).
- Осознанно **не** UTM: Метрика берёт метку из первого просмотра визита, а
  нестандартный `utm_source` относит визит к «другой поисковой системе». На
  внутренней ссылке метка либо игнорируется, либо переписывает исходный канал
  (поиск, ChatGPT, рассылку). UTM остаются внешним каналам — рассылкам.
- `trial_register_click` в демо теперь передаёт `source` и `version_id`:
  раньше оба вызова шли с одним `destination`, и разделить «верх» и «низ»
  страницы было нельзя.
- Из `src/lib/metrika.ts` убрана мёртвая цель `hero_chat_version_switch`:
  константа была объявлена, но не вызывалась ниоткуда — переключателя версий
  в `Hero.tsx` нет, он живёт в демо, тарифах и `VersionPicker`, у которых свои
  рабочие цели. В Метрике цель 599836961 можно архивировать.
- `docs/lk/metrika-csp-blocker.md` — сообщение команде ЛК: диагноз, обе
  директивы CSP, два способа решить проблему с инлайн-кодом, просьба не терять
  `from`/`plan`/`version` при редиректах и чеклист проверки.

Материалы для итерации 4 — `docs/ssylki-materialy.md`: четыре готовых письма в
редакции (АТОР и Hotel.Report с опечаткой «Сигаладзе», Inc.Russia, tourismexpo,
ТПП/РСТ), единое описание для каталогов, реквизиты, список площадок.

Там же зафиксирован конфликт: ссылка в сниппете виджета противоречит обещанию
white-label, которое стоит примерно в двенадцати местах на сайте и в llms.txt.
Предложены три варианта, рекомендация — просить ссылку у партнёров на их
страницах «Партнёры/Технологии», а подпись в виджете делать позже как opt-in
со скидкой.

## 8д. Итерация 5 — широкие запросы (13.09.2026)

Закрыты два пробела из раздела 6, где нас не было или мы были вторыми.

**`/cifrovizaciya-turagentstva`** — pillar-страница, ~2 380 слов. Отличается от
статьи `ii-v-turizme-2026` намеренно: там ИИ в туризме, здесь ИИ — только один
из шести процессов, которые агентство переводит в цифру (приём обращений,
подбор, заявки и CRM, документы, работа с базой, аналитика). По каждому блоку:
что болит, что ставят, порядок цен на рынке и честная отметка, где наш продукт
задачу не решает — блок «Документы и оплата» оставлен без нашей части.
Дополнительно: порядок внедрения из четырёх шагов с признаком закрытия каждого,
чеклист готовности на десять пунктов, смета минимального набора, пять типовых
ошибок, глоссарий из двенадцати терминов и восемь внешних источников.

Разметка: `Article`, `HowTo` на порядок внедрения, **`DefinedTermSet`** на
глоссарий и `FAQPage` на шесть вопросов. `DefinedTermSet` выбран сознательно:
определения терминов модели цитируют охотнее прозы, а такой разметки у
конкурентов в нише нет.

**`/blog/kak-uvelichit-prodazhi-turagentstva`** — статья, ~1 320 слов, семь
рычагов роста от самого дешёвого к самому долгому, с таблицей «усилие → когда
виден эффект» и разделом «три способа потратить деньги зря». Запрос «как
турагентству увеличить продажи» в замере №2 был у нас пустым, топ занимали
amsales, turmarketing и блоги CRM.

Данные в обеих страницах — только проверенные: АТОР («ТрЭволюция», 05.09.2026)
о доле агентских продаж 95% → 85% и ~2% готовых доверить бронь ИИ, Mediascope
об аудитории MAX, «Слетать.ру» о 89% возвратов и десяти минутах ожидания,
Fun&Sun о 70% продаж от постоянных клиентов, прайсы Tourvisor и U-ON,
BotHelp о стоимости разработки. Цены сторонних сервисов вынесены в
`src/lib/seo/digital-pillar.ts` вместе с датой проверки, чтобы обновлять их в
одном месте.

Перелинковка: пункт в футере и карте сайта, sitemap и `gen-lastmod.py`,
llms.txt и llms-full.txt, взаимные ссылки с `/resheniya`, `ii-v-turizme-2026`,
`kak-vozvraschat-klientov-turagentstvu`, `/skolko-stoit` и `/sravnenie`.

Материалы итерации 4 дополнены письмом партнёрам (`docs/ssylki-materialy.md`,
раздел 4.1): решение по конфликту с white-label — просим ссылку у партнёров на
их страницах «Партнёры/Технологии», подпись в виджете не добавляем.

## 8е. Пересмотр приоритетов после сверки с практиками 2026 (16.09.2026)

Проведена сверка с первоисточниками Google, Яндекса и OpenAI и замер восьми
сайтов конкурентов. Подробности — в `geo-praktiki-2026.md` и
`konkurenty-2026-09-16.md`. Ниже только то, что меняет план.

### Поправки к прошлым решениям

- **Диагноз «хостер урезал канал» (15.09) неверен.** Сервер отдаёт себе главную
  за 0,03 с и грузит наружу на 23 МБ/с, ограничений на интерфейсе нет. RTT от
  машины разработчика до сервера — 225 мс, медленно только на этом маршруте.
  У российских пользователей сайт работает нормально.
- **`FAQPage` и `HowTo` не дают rich results.** Google отключил FAQ-сниппет
  7 мая 2026 и удалил документацию 15 июня; HowTo мёртв с 2023 года. В Яндексе
  Q&A-разметка для мобильных сниппетов ещё используется, поэтому убирать не
  нужно, но новые страницы размечать ради сниппетов бессмысленно.
- **`DefinedTermSet` на глоссарии `/cifrovizaciya-turagentstva` не поддержан
  Google в принципе.** Обоснование «модели цитируют определения охотнее» было
  ничем не подтверждено. Разметка валидна, вреда нет, но эффекта ждать не надо.
- **llms.txt Google официально игнорирует.** Файлы у нас корректны и лучше,
  чем у большинства конкурентов, но развивать их как приоритет не нужно.
- **Ссылки — не главный GEO-рычаг.** По Ahrefs на 75 000 брендов упоминания
  бренда дают корреляцию 0,66–0,71, бэклинки — 0,22. Правильная цель —
  упоминания и попадание в рейтинги; ссылка это частный случай.

### Что подтвердилось

Направление на сравнительные страницы выбрано верно: сравнительные запросы
дают в 2,4 раза больше упоминаний бренда. Собственный контент тоже: 57% всех
ИИ-цитирований приходится на сайты брендов. Открытые цены и `SoftwareApplication`
с `Offer` — наша сильная сторона, в нише её почти ни у кого нет.

### Найдено и исправлено 16.09

`logo-short.svg`, `globe-logo.svg` и `logo-icon.svg` были байт в байт одним
файлом, браузер качал его трижды. Три логотипа на 604 КБ стояли в
`<link rel="preload">` и забивали канал раньше шрифта и скриптов. После
дедупликации и svgo: логотипы 1213 → 515 КБ, вес главной 671 КБ,
**mobile LCP 12,2 → 4,3 с** (замер Lighthouse со сжатием). Коммит `095ba0e`.

### Новая очередь работ

| № | Что | Почему именно это | Чьё |
|---|---|---|---|
| 1 | Отзывы на сторонних площадках + `/otzyvy` + `AggregateRating` | Единственный неиспользованный тип разметки с официальной поддержкой; требование Яндекса (ЭПОС); UGC-рейтинги 88,9% у практиков. У u-on.ru 70+ отзывов при нулевой технике | Ваше + моё |
| 2 | Попадание в тематические подборки и рейтинги, публикации | Сильнейший измеренный коррелят 0,66–0,71. Письма готовы | Ваше |
| 3 | Карточка в Яндекс Бизнесе, инструмент «Видимость в Алисе AI» | Официальные пункты Яндекса; единственный легальный способ мерить GEO в РФ | Ваше |
| 4 | Страница эксперта с `Person` + `sameAs`, авторство статей | Блок «Экспертность» ЭПОС | Моё |
| 5 | «Индекс спроса» на данных базы | Собственные исследования — 70% у практиков; non-commodity content у Google | Моё |
| 6 | Кейсы 1 → 4 | Портфолио по ЭПОС; у nextbot.ru их 25 | Моё + ваши цифры |
| 7 | LCP 4,3 → 2,5 с: вес JS, framer-motion, отложенный демо-виджет | Обычное ранжирование и конверсия, не GEO | Моё |

Снято с плана: наращивание llms.txt, новые `FAQPage`/`HowTo` ради сниппетов,
расширение pSEO-кластеров (риск scaled content abuse), ИКС как KPI, ставка на
Bing ради ChatGPT (у OpenAI свой индекс, пересечение с топ-20 Bing 1,5%).

## 8ж. Замер результатов через 12 дней (25.09.2026)

Сравнение 30.08–12.09 (до контентных правок) и 13.09–24.09 (после), Метрика,
роботы отфильтрованы.

| Показатель | До, в день | После, в день |
|---|---|---|
| Визиты | 16,5 | 13,8 |
| Органика | 6,2 | **7,1 (+15%)** |
| Заявки через форму, всего | 3 за 14 дней | 3 за 12 дней |
| Клик «регистрация», людей | 21 | 3 |
| Открыли чат в демо, людей | 34 | 12 |

Что видно:

- Общее падение визитов — это прямой трафик: в период «до» входили визиты
  команды 3 и 8 сентября. Честная метрика — органика, и она выросла.
- **Новые страницы начали получать поиск за 12 дней:** `/chat-bot-dlya-turagentstva`
  5 входов из поиска, `/max` 4, `/vidzhet` 3. Появились новые запросы: «бот макс
  для туристического агентства», «бот для турагента», «ии сервисы для
  турагентств», «ии в туризме» ×4.
- **`/blog/ii-v-turizme-2026` стал третьей посадочной сайта** (19 входов) после
  обновления 12.09 с данными АТОР и Mediascope.
- ИИ-источники: chatgpt.com 3, alice.yandex.ru 1.
- Клики регистрации начали падать 10.09, до деплоев. Проверено на проде
  25.09: цели `demo_chat_open` и `trial_register_click` отправляются, в том
  числе до перехода в кабинет; метка `from=` доходит; виджет грузится. Поломки
  нет — это состав трафика.
- **CSP кабинета не исправлен:** за 12 дней на `lk.navilet.ru` 2 просмотра.
  Сколько людей зарегистрировалось — по-прежнему неизвестно.

Вывод: SEO-часть даёт медленный рост органики, как и ожидалось на горизонте
двух недель. Главный результат — заявки и регистрации — мы либо не видим
(кабинет), либо он не растёт (форма). Следующие шаги должны бить в это.

## 8з. Регистрации по базе кабинета (25.09.2026) — поправка к выводу 8ж

В 8ж сказано, что бизнес-результат не сдвинулся. Это было неполно: вывод
опирался на форму заявки, а регистрации в кабинете Метрика не видит из-за CSP.
Таблица `signup_requests` показывает их напрямую.

| Месяц | Одобрено | Черновики | Отклонено |
|---|---|---|---|
| Июль | 4 | 10 | 4 |
| Август | 7 | 3 | 1 |
| Сентябрь (по 25.09) | 13 | 3 | 2 |

38 из 48 заявок на регистрацию пришли с navilet.ru, 2 — с e.mail.ru.

**Откуда сентябрьский рост.** 12 из 13 одобренных — с 4 по 8 сентября, и
каждая из них с отдельного IP, от отдельной компании с отдельным сайтом, то
есть это 12 разных агентств, а не действия команды. Две пришли с e.mail.ru —
всплеск дала рассылка. В Метрике она была видна как прямой трафик: в письмах
не было UTM-меток. После 13.09 — одна одобренная регистрация и одна на
проверке.

**Вывод.** Рассылка дала 12 регистраций за пять дней — больше, чем органика за
месяц. SEO растит органику медленно, как и ожидалось, а регистрации сейчас
делает исходящий канал. Два практических следствия:

1. Все письма — только с UTM (`utm_source=email&utm_medium=newsletter&utm_campaign=<дата>`),
   иначе лучший канал продолжит выглядеть как «прямые заходы».
2. Метка `from=` на переходах в кабинет не доходит до базы: `referrer` режется
   политикой браузера до `https://navilet.ru/`. Кабинет должен сохранять
   `from` из своего URL — добавлено пунктом 4 в `docs/lk/metrika-csp-blocker.md`.

Уточнение к 2-му разделу аудита: там всплески 03.09 и 08.09 отнесены к визитам
команды по логам nginx. Для просмотров страниц это может быть верно, но
регистрации в эти дни — реальные разные агентства.

## 8и. Индекс спроса и сверка /spros с базой (25.09.2026)

**Что нашлось.** Цифры на `/spros/*` (с июля) базой не подтверждались:

| Утверждение на сайте | В базе (июнь — 24.09) |
|---|---|
| ОАЭ ≈14%, «третье место» | 1% за всю историю, включая весну |
| Вьетнам 7%, тренд −4,6 | 11% — третье место |
| Мальдивы 6%, «до трети — медовый месяц» | 3%; медовый месяц упоминают <5% |
| Египет: чек 145 тыс., «короткий горизонт» | 170 тыс.; Египет ищут на 2 недели раньше Турции |
| Турция: «78% хотят 5★», «окно решения 15–20 минут» | звёздность подставляет бот; про окно данных нет |
| Таиланд: «зимовка на месяц» | от 14 ночей — 9%, от 21 — 1% |

**Что сделано.** `/indeks-sprosa` — ежемесячный выпуск (Article + Dataset);
все цифры `/spros` берутся из `src/lib/seo/demand-index.ts`; тексты
переписаны на проверенные факты; `/spros/oae` → 301 на индекс. По решению
владельца на сайте только доли и медианы, без числа диалогов; о концентрации
данных в методике — без деталей.

**Проверки, на которых держится индекс.** Названия стран Tourvisor взяты из
словаря бэкенда кабинета и сверены с текстом диалогов (83–98%, Россия 59% —
пишут курорт). Бюджет, дети и ночи подтверждаются текстом в 77–98%, звёздность —
в 7–14% (исключена). Порядок пятёрки стран одинаков с крупнейшим агентством и
без него; рост Египта виден в обеих группах. Города вылета и месячные объёмы
не публикуются — зависят от состава агентств.

**Обновление раз в месяц** (≈30 минут):

1. `scripts/demand-index.sql` с новыми `start`/`cur`/`end` (команда в шапке файла).
2. Перенести цифры в `demand-index.ts`: `edition`, `published`, `periodTo`,
   `periodLabel`, `trendLabel`, доли, медианы, месяцы.
3. Прочитать тексты `demand-pages.ts`: сравнения («максимум среди…», «вдвое
   чаще…») и курорты зашиты в текст — проверить, что всё ещё верны.
4. Сборка, локальная проверка, деплой.

**Как мерить результат.** Индекс работает через упоминания, а не трафик:
поисковые запросы «спрос на туры», «куда едут туристы» в Вебмастере; переходы и
цель `trial_register_click` с `indeks_sprosa_*`; упоминания «индекс спроса
Навылет» в Яндексе и ответах Алисы — первый замер через месяц после выпуска.

## 9. Предлагаемая очередь работ (историческая, см. 8е)

| Итерация | Что | Эффект |
|---|---|---|
| 1. Техника (1 день) | H1 с пробелами, hero без opacity:0, 301 для `.html`/`index.html`/IP, dateModified МГП, descriptions ≤160, robots, автогенерация lastmod и даты llms.txt из git, RSS, alt/aria, founder + sameAs, Person-автор | LCP → ~2 с, чистый индекс, доверие к датам |
| 2. Контент-ядро (1–2 недели) | `/chat-bot-dlya-turagentstva`, `/max`, сравнительная страница, усиление `/vidzhet` и `/resheniya`, статья «как возвращать клиентов», источники в блог | захват 4 из 5 потерянных тем |
| 3. Ссылки (параллельно) | статичная ссылка в виджете, правки в СМИ, Сколково, каталоги, Яндекс Бизнес, Pressfeed | первые внешние ссылки и отзывы |
| 4. Данные и PR (ежемесячно) | «Индекс спроса», 2 кейса, страница эксперта, замер видимости №3 | цитируемость в СМИ и моделях |
